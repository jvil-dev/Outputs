# Legacy Electron app

This is the reference implementation of Outputs. The Swift app at the repo root is being built to match it.

Run it with `npm install && npm start`. It will be removed once the Swift app reaches feature parity.
